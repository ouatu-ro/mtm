[project]
name = "mtm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
mtm-demo = "mtm.demo:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
